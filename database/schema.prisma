// database
datasource db {
  provider = "<your database name>" //here put your database name eg. cockrockDB, sqlite
  url      = "<put here the url of your database + password>"
}

// generator
generator client {
  provider = "prisma-client-py"
}
